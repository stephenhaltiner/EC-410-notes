Functions in R: Intro
================

## Basic syntax

Create your own function:

    my_func <- 
    function(ARGUMENTS) {
      OPERATIONS
      return(VALUE)
    }

``` r
my_short_func <- function(ARGUMENTS) OPERATION
```
